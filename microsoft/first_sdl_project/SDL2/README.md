# MSVC SDL files

## dll - holds all the SDL2XXX.dll files
## lib - holds the dynamic and static file libraries
## include/SDL2 - holds all the include files for SDL2 programming

This is slightly different than the Code::Blocks version because we plan on 
copying these folders **directly** into our project as is.
